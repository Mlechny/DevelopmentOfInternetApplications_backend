<!DOCTYPE html>
<html>
<head>
	<link href="/styles/card-container.css" rel="stylesheet" type="text/css">
	<link href="/styles/sliding-button.css" rel="stylesheet" type="text/css">
</head>
<body>
	<h1>Список языков программирования</h1>
	<p>
	<form action="/languages" method="get">
	<input type="text" name="name" value="{{if ne .Name ""}}{{.Name}}{{end}}" placeholder="Введите название ЯП">
    <button class="sliding-button" type="submit">Поиск</button>
    </form>
    </p>
	<div class="card-container">
	{{range $key, $value := .Languages}}
			<div class="card-container__card">
				<div class="card-container__img">
					  <img src={{$value.ImageURL}} alt="" />
				</div>
				<div class="card-container__name">
					<h3>{{$value.Name}}</h3>
				</div>
				<div class="card-container__subject">
					<p>Предмет: {{$value.Subject}}</p>
				</div>
				{{if not .IsDeleted}}
				<form action="/languages" method="post">
                     <input type="hidden" name="delete" value="{{.LanguageId}}">
                     <input type="submit" value="Удалить" class="sliding-button">
                </form>
                {{end}}
                 <a href="/languages/{{$value.LanguageId}}" class="sliding-button">
                 Подробнее
                 </a>
			</div>
		{{end}}
	</div>
</body>
</html>